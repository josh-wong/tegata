package vault

// GenerateRecoveryKey generates a 32-byte random recovery key and returns
// the raw bytes and a human-readable base32-encoded display string.
func GenerateRecoveryKey() (raw []byte, display string, err error) {
	panic("not implemented")
}
